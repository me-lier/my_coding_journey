#include<iostream>
using namespace std;
int main(){
    char ch;
    cout<<"Enter any character to get  the  ASCII value :- ";
    cin>>ch;
    cout<<"The ASCII value of :- "<< ch <<" is = "<<(int(ch));
}
